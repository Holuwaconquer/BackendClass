services:
  - type: web
    name: backendclass
    env: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: URI
        sync: false
      - key: SECRET_KEY
        sync: false
      - 